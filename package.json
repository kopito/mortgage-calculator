{
  "name": "mortgage-calculator",
  "version": "1.0.0",
  "description": "Mortgage Amortization Calculator with Offset Account",
  "main": "index.html",
  "scripts": {
    "dev": "npx http-server . -p 8080",
    "build": "echo 'No build step needed for static app'",
    "android:sync": "npx cap sync android",
    "android:open": "npx cap open android",
    "android:build": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "keywords": ["mortgage", "calculator", "pwa"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@capacitor/cli": "^5.5.0",
    "@capacitor/android": "^5.5.0"
  }
}
